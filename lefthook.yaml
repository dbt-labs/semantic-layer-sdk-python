pre-commit:
  commands:
    ruff-format:
      run: "hatch run dev:ruff format"
    ruff-lint:
      run: "hatch run dev:ruff check --fix"
    pyright:
      run: "hatch run dev:basedpyright"

commit-msg:
  scripts:
    "template-checker":
      runner: bash
