pre-commit:
  commands:
    ruff-format:
      run: .venv/bin/ruff format
    ruff-lint:
      run: .venv/bin/ruff check --fix
    pyright:
      run: .venv/bin/basedpyright

commit-msg:
  template-checker:
    runner: bash
