pre-commit:
  commands:
    ruff-format:
      run: ruff format
    ruff-lint:
      run: ruff check --fix
    pyright:
      run: basedpyright

commit-msg:
  scripts:
    "template-checker":
      runner: bash
